
"use client";

// This page was used for a one-time data import and is no longer needed.
export default function ImportPage() {
  return (
     <div className="flex flex-col items-center justify-center min-h-screen bg-background p-4">
        <h1 className="text-2xl font-bold">Import Complete</h1>
        <p className="text-muted-foreground">The user data has already been seeded into the database.</p>
     </div>
    );
}
